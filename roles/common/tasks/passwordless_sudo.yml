---
- name: SUDO | Ensure passwordless sudo for current user
  become: true
  ansible.builtin.copy:
    dest: "/etc/sudoers.d/{{ ansible_user_id }}"
    content: "{{ ansible_user_id }} ALL=(ALL) NOPASSWD:ALL"
    owner: root
    group: root
    mode: '0440'
