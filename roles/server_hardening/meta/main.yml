---
galaxy_info:
  role_name: server_hardening
  author: opskern
  description: >
    Standardized server hardening for Ubuntu 24.04. Covers SSH, UFW, fail2ban,
    unattended-upgrades, sysctl kernel hardening, NTP, systemd defaults,
    user management, and login banner.
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: Ubuntu
      versions:
        - jammy
        - noble

dependencies:
  - role: common
